# Core API
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
python-multipart>=0.0.9

# Numerics and data
numpy>=1.24,<3.0
pandas>=2.0
scipy>=1.11

# Machine learning
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3

# Multi-objective quality indicators (hypervolume / IGD).
# Optional: benchmarking/metrics.py falls back to built-in implementations.
pymoo>=0.6

# Optional deep-learning backend for model_type="neural_network".
# Large download; install only if you need it.
# torch>=2.0

# Tests
pytest>=8.0
httpx>=0.27
